
› when i test order faild to sync and inventorymovement also failed


• I’ll trace the order and inventory-movement outbox payloads against the server’s required request shapes and ID-mapping rules. These failures commonly happen when a local product/variant/store/session ID reaches the server before it
  has been reconciled.

• Ran rg -n -C 5 "case \"orders\"|case \"inventory_movements\"|createOfflineOrder|enqueueMutation\(|createLocalMovement|createRemoteOrder|createRemoteInventoryMovement" services\offline\syncManager.ts services\offline\repository.ts
  │ services\features\offline\localApi.ts services\api\remoteApi.ts; Get-Content ..\pos_server\src\routes\orders.ts | Select-Object -First 310; Get-Content ..\pos_server\src\routes\inventory.ts | Select-Object -Skip 95 -First 200
  └ import { Elysia, t } from "elysia";
    import { prisma } from "../lib/prisma";
    … +503 lines (ctrl + t to view transcript)
    services\api\remoteApi.ts: IO error for operation on \inventory_movements\|createOfflineOrder|enqueueMutation\(|createLocalMovement|createRemoteOrder|createRemoteInventoryMovement services\offline\syncManager.ts
    services\offline\repository.ts services\features\offline\localApi.ts services\api\remoteApi.ts: The system cannot find the path specified. (os error 3)

• Ran Select-String -Path services\offline\syncManager.ts -Pattern 'case "orders"','case "inventory_movements"','createRemoteOrder','createRemoteInventoryMovement' -Context 5,60; Select-String -Path services\offline\repository.ts
  │ -Pattern 'createOfflineOrder','inventory_movements','enqueueMutation' -Context 4,28; Select-String -Path 'app\(tabs)\index.tsx' -Pattern 'createLocalOrder','createLocalInventoryMovement','createMovement' -Context 5,30
  └
      services\offline\syncManager.ts:1202:      }
    … +1892 lines (ctrl + t to view transcript)



• Ran Get-Content 'app\(tabs)\index.tsx' | Select-Object -Skip 250 -First 125; Get-Content services\offline\syncManager.ts | Select-Object -Skip 1560 -First 115; Select-String -Path services\offline\repository.ts -Pattern '^export async
  │ function createOfflineOrder','^export async function createOfflineInventoryMovement' -Context 0,180
  └     Animated.spring(slideAnim, {
          toValue: height,
    … +607 lines (ctrl + t to view transcript)



─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• I found the main fault: checkout was creating an order and then separately creating a sale inventory movement. The server already creates the authoritative sale movement while processing the order, so the second sync tries to deduct
  the same stock again and fails. I’m making the order the single source of truth and automatically retiring those old duplicate movement records once their order has synced.

• Edited 2 files (+18 -27)
  └ akhayar-pos-app\app\(tabs)\index.tsx (+3 -23)
     12  import {
     13 -  useCreateLocalInventoryMovementMutation,
     13    useCreateLocalOrderMutation,
        ⋮
    146    const [createOrder] = useCreateLocalOrderMutation();
    148 -  const [createInventoryMovement] = useCreateLocalInventoryMovementMutation();
    147
        ⋮
    325
    328 -      for (const item of cartItems) {
    329 -        await createInventoryMovement({
    330 -          tenantId: user?.tenantId,
    331 -          storeId: activeSession.storeId,
    332 -          productId: item.productId || item.id,
    333 -          // If the backend still stores shared product stock, send the
    334 -          // movement against the product row while keeping variantId on the
    335 -          // order item for reporting.
    336 -          variantId: hasSeparatedVariantInventory(
    337 -            item.productId || item.id,
    338 -            item.variantId,
    339 -          )
    340 -            ? item.variantId
    341 -            : undefined,
    342 -          quantity: item.qty,
    343 -          type: "OUT",
    344 -          referenceId: result.id,
    345 -          referenceType: "ORDER",
    346 -          reason: `Order #${result.orderNumber || result.id}`,
    347 -        }).unwrap();
    348 -      }
    326 +      // The order sync endpoint deducts stock and writes the authoritative
    327 +      // SALE movement on the server. Do not enqueue a second movement here:
    328 +      // it would deduct the same quantity twice when the device reconnects.
    329

  └ akhayar-pos-app\services\offline\syncManager.ts (+15 -4)
    1589
    1590 -            movementPayload = {
    1591 -              ...payload,
    1592 -              referenceId: order.remoteId ?? payload.referenceId,
    1593 -            };
    1590 +            // Server order creation already writes the SALE/RETURN stock
    1591 +            // movement atomically. Older mobile builds queued a duplicate
    1592 +            // movement after checkout; retire it rather than applying the
    1593 +            // stock delta twice.
    1594 +            await db
    1595 +              .update(inventoryMovements)
    1596 +              .set({
    1597 +                syncStatus: "synced",
    1598 +                syncError: null,
    1599 +                lastSyncedAt: new Date().toISOString(),
    1600 +                updatedAt: new Date().toISOString(),
    1601 +              })
    1602 +              .where(eq(inventoryMovements.id, item.entityId));
    1603 +            await markOutboxSynced(item.id);
    1604 +            return { success: true };
    1605            }

• Edited akhayar-pos-app\app\(tabs)\index.tsx (+13 -9)
    164    });
    165 +  const hasUnallocatedVariantStock = cartItems.some(
    166 +    (item) =>
    167 +      !!item.variantId &&
    168 +      !hasSeparatedVariantInventory(item.productId || item.id, item.variantId),
    169 +  );
    170
        ⋮
    272      }
    273 +    if (hasUnallocatedVariantStock) {
    274 +      Alert.alert(
    275 +        "Allocate option stock first",
    276 +        "This product has options, but its stock is still stored on the master product. Allocate the stock to each option before selling it.",
    277 +      );
    278 +      return;
    279 +    }
    280      if (!activeSession) {
        ⋮
    316          })),
    305 -        // The local order keeps variantId for history. If the backend only
    306 -        // has product-level inventory, its stock validation must receive the
    307 -        // product row instead of a non-existent variant stock row.
    317          syncItems: cartItems.map((item) => ({
    318            productId: item.productId || item.id,
    310 -          variantId: hasSeparatedVariantInventory(
    311 -            item.productId || item.id,
    312 -            item.variantId,
    313 -          )
    314 -            ? item.variantId
    315 -            : undefined,
    319 +          variantId: item.variantId,
    320            quantity: item.qty,

⚠ Heads up, you have less than 10% of your monthly limit left. Run /status for a breakdown.


• Edited akhayar-pos-app\services\offline\syncManager.ts (+16 -16)
    1249          if (item.operation === "create") {
    1250 -          // ✅ Check if session is still open locally
    1251 -          const [session] = await db
    1252 -            .select()
    1253 -            .from(sessions)
    1254 -            .where(eq(sessions.id, payload.sessionId))
    1255 -            .limit(1);
    1256 -
    1257 -          if (!session || session.status !== "OPEN") {
    1258 -            const error = `Session ${payload.sessionId} is closed. Please reopen a session and retry.`;
    1259 -            console.warn(`⚠️ ${error}`);
    1260 -            await markOrderSyncFailed(item.entityId, error);
    1261 -            await markOutboxDead(item.id); // Stop retrying
    1262 -            return { success: false, error };
    1263 -          }
    1264 -          if (!session.remoteId) {
    1250 +          // A sale made before an offline session closes is still valid. Use
    1251 +          // the session only when it has already reached the server and is
    1252 +          // still open; otherwise submit the sale without a session instead
    1253 +          // of permanently dead-lettering the order.
    1254 +          const [session] = payload.sessionId
    1255 +            ? await db
    1256 +                .select()
    1257 +                .from(sessions)
    1258 +                .where(eq(sessions.id, payload.sessionId))
    1259 +                .limit(1)
    1260 +            : [];
    1261 +          if (session?.status === "OPEN" && !session.remoteId) {
    1262              return {
    1271 +            sessionId:
    1272 +              session?.status === "OPEN" && session.remoteId
     90 +      if (body.orderNumber) {
     91 +        const existingOrder = await prisma.order.findFirst({
     92 +          where: { tenantId, orderNumber: body.orderNumber, deletedAt: null },
     93 +          include: { items: true },
     94 +        });
     95 +        if (existingOrder) {
     96 +          return { success: true, message: "Order already synced.", order: existingOrder };
     97 +        }
     98 +      }
     99
        ⋮
    182
    171 -      const orderNumber = `ORD-${Date.now()}`;
    183 +      const orderNumber = body.orderNumber ?? `ORD-${Date.now()}`;
    184
        ⋮
    279          paidAmount: t.Number(),
    268 -        changeAmount: t.Number(),
    280 +        changeAmount: t.Number(),
    281 +        orderNumber: t.Optional(t.String()),
    282 +        clientOrderId: t.Optional(t.String()),
    283          customerId: t.Optional(t.String()),

⚠ Heads up, you have less than 5% of your monthly limit left. Run /status for a breakdown.

• Ran Select-String -Path services\offline\repository.ts -Pattern '^export async function markOutboxDead','^export async function getDueOutboxItems','^export async function getFailedOutboxItems','^export async function retryOutboxItem'
  │ -Context 0,45; Select-String -Path services\offline\syncManager.ts -Pattern 'markOutboxDead' -Context 2,2; Select-String -Path ..\pos_server\src\routes\orders.ts -Pattern 'orderNumber|clientOrderId|Order already synced' -Context
  │ 2,2
  └
    > services\offline\repository.ts:2654:export async function getDueOutboxItems(limit = 25) {
    … +206 lines (ctrl + t to view transcript)



• Edited 2 files (+8 -9)
  └ akhayar-pos-app\services\offline\syncManager.ts (+0 -2)
    46    upsertSuppliers,
    47 -  markOrderSyncFailed,
    48 -  markOutboxDead,
    47    upsertOrders,

  └ pos_server\src\routes\orders.ts (+8 -7)
    131
    132 -      // Fallback: Resolve or create "default-register" via upsert
    133 -      if (!registerId) {
    134 -        const defaultReg = await prisma.cashRegister.upsert({
    135 -          where: { id: "default-register" },
    136 -          update: { status: "OPEN" },
    137 -          create: {
    138 -            id: "default-register",
    132 +      // Fallback: Resolve or create "default-register" via upsert
    133 +      if (!registerId) {
    134 +        const defaultRegisterId = `default-register-${body.storeId}`;
    135 +        const defaultReg = await prisma.cashRegister.upsert({
    136 +          where: { id: defaultRegisterId },
    137 +          update: { status: "OPEN" },
    138 +          create: {
    139 +            id: defaultRegisterId,
    140              tenantId,